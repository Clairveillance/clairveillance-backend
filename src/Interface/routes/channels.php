<?php

declare(strict_types=1);

namespace Interface\routes;

use Interface\Support\Facades\Broadcast;

Broadcast::channel('App.Models.User.{id}', function ($user, $id) {
    return (int) $user->id === (int) $id;
});
